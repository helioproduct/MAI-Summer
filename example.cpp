#include <bits/stdc++.h>

using namespace std;

void print_vec(vector<double> vec, int z)
{
    cout.precision(z);
    for (double el : vec) {
        cout << fixed << el << '\n';
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    vector<double> a = {1, 2, 3, 4};

    return 0;
}